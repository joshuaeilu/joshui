import { IonFooter, IonIcon, IonRange, IonRow, IonText, IonTitle, IonToolbar } from '@ionic/react';
import './Footer.css';

function Footer() {
  return (
    <IonFooter>
      <IonToolbar>
        <IonTitle>Title of Wheel</IonTitle>
        <IonRow>
          
        </IonRow>
      </IonToolbar>
    </IonFooter>
  )
}

export default Footer;
